server:
  port: 7003
eureka:
  instance:
    hostname: localhost #eureka名称
  client:
    register-with-eureka: false
    fetch-registry: false # 为false,表示为注册中心
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/,http://eureka7001.com:7001/eureka/
